export const molecules = [
    // This file can be used for 3D molecule definitions if they exist in the future.
    // Currently, molecule references are embedded in the content.ts sections, 
    // but we can move them here if better isolation is needed.
];
